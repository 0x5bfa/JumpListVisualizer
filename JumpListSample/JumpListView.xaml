<!--  Copyright (c) 0x5BFA. Licensed under the MIT License.  -->
<UserControl
	x:Class="JumpListSample.JumpListView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="using:Microsoft.Xaml.Interactivity"
	xmlns:local="using:JumpListSample"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Width="300"
	Loaded="JumpListView_Loaded"
	mc:Ignorable="d">

	<UserControl.Resources>

		<x:Double x:Key="ListViewItemMinHeight">32</x:Double>

		<local:JumpListItemTemplateSelector
			x:Key="JumpListItemTemplateSelector"
			Item="{StaticResource JumpListItemTemplate}"
			SectionItem="{StaticResource JumpListSectionItemTemplate}" />

		<DataTemplate x:Key="JumpListSectionItemTemplate" x:DataType="local:JumpListSectionItem">
			<Grid Margin="12,7,12,6" ColumnSpacing="8">
				<TextBlock
					Foreground="{ThemeResource TextFillColorSecondaryBrush}"
					Style="{StaticResource CaptionTextBlockStyle}"
					Text="{Binding Text}"
					TextTrimming="CharacterEllipsis"
					TextWrapping="NoWrap" />
			</Grid>
		</DataTemplate>

		<DataTemplate x:Key="JumpListItemTemplate" x:DataType="local:JumpListItem">
			<Grid Margin="12,7,24,6">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="16" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<Image Source="{x:Bind Icon}" />
				<TextBlock
					Grid.Column="1"
					Margin="12,0"
					Text="{Binding Text}"
					TextTrimming="CharacterEllipsis"
					TextWrapping="NoWrap" />
			</Grid>
		</DataTemplate>

	</UserControl.Resources>

	<Grid RowSpacing="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" ColumnSpacing="12">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<TextBox
				x:Name="AppIdTextBox"
				Grid.Column="0"
				IsSpellCheckEnabled="False"
				PlaceholderText="Enter AMUID..."
				Text="{x:Bind ViewModel.AppId, Mode=TwoWay}"
				TextWrapping="NoWrap" />
			<Button
				Grid.Column="1"
				Command="{x:Bind ViewModel.RefreshJumpListCommand}"
				Content="View" />
		</Grid>

		<ScrollView
			Grid.Row="1"
			Height="600"
			MaxHeight="600"
			Padding="0,0"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="{StaticResource OverlayCornerRadius}">

			<ItemsControl ItemTemplateSelector="{StaticResource JumpListItemTemplateSelector}" ItemsSource="{x:Bind ViewModel.JumpListItems, Mode=OneWay}" />

		</ScrollView>

	</Grid>
</UserControl>
